# SQL Scripts

## Tables
- `create_tables.sql` — All CREATE TABLE statements
- `constraints.sql` — Business rule constraints and triggers
- `queries.sql` — Sample analytical queries

## How to Run
1. Run `create_tables.sql` first to create all tables
2. Run `constraints.sql` to apply business rules
3. Run `queries.sql` to test sample queries
